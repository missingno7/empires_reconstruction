# Matching C wave 91

`F_B122` adds its complete 693-byte return-terminated state routine as a
freshly compiled matching-C owner. The source preserves every instruction byte
explicitly and introduces no external fixups or loader relocations. Fresh
compile, bind, and full-game equality all pass; raw executable fallback is now
28,165 bytes.
