# Wave 112 matching-C conversion proof

F_6B1A is now a matching-C owner. Turbo C naturally emits the original SI
preservation around the inline BIOS keyboard poll. The fresh object reproduces
both near-call fixups and all 48 bytes of the prior ASM extent, with no loader
relocations.

Matching-C coverage is now 56,439 bytes across 326 owners; matching-ASM
coverage is 2,456 bytes across 19 owners.
