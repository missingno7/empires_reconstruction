# Matching-C wave 118

F_C1F7 is now a matching-C owner. Its complete 59-byte sound voice pump
compiles with Turbo C, preserving the historical frame, six data references,
two calls and no DOS loader relocations. The fresh object matches the prior
ASM extent byte for byte.

Matching-C coverage is now 56,994 bytes across 332 owners. Matching-ASM
coverage is 1,901 bytes across 13 owners.
