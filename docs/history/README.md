# Historical evidence

All older wave reports, checkpoints, probe receipts and experiment descriptions
outside `docs/current/` are historical evidence. Their original paths are retained
because tests and provenance records link to them. They are not current blockers
or a grinder queue. No useful evidence was deleted or silently rewritten.

The authoritative current answer is `../current/status.json`; the next task is
`../current/grinder-queue.json`. Current files are generated by
`tools/reconstruction_factory.py`; the build plan is `layout/production-plan.json`.

The workflow audit is `../current/workflow-audit.md`. In particular, the old
natural-public and exact-link checkpoints describe intermediate adapter states
which later commits removed. Read those reports as dated experiment records.
