# Matching-C wave 124

F_C1A0 is now a matching-C owner. Its complete 87-byte master sound tick
compiles from a deterministic Turbo C source unit, preserving the historical
save order, two calls, five data symbols and no DOS loader relocations. The
fresh object matches the prior ASM extent byte for byte.

Matching-C coverage is now 57,489 bytes across 338 owners. Matching-ASM
coverage is 1,406 bytes across 7 owners.
