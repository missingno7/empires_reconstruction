# Matching-C wave 115

F_1F91 is now a matching-C owner. Its complete 126-byte span clamp and row
accumulator compiles with Turbo C, preserving the compiler-generated SI/DI
frame, one far data fixup, and no DOS loader relocations. The fresh object
matches the prior ASM extent byte for byte.

Matching-C coverage is now 56,741 bytes across 329 owners. Matching-ASM
coverage is 2,154 bytes across 16 owners.
