# Documentation map

- **Current status and next work:** `current/status.json`, `current/grinder-queue.json`.
- **Cheap-model procedure:** `current/grinder-instructions.md`.
- **Rules and supervision:** `current/tasm-reconstruction-rules.md`, `current/supervisor-instructions.md`.
- **Historical evidence:** see `history/README.md`. Existing checkpoint and wave
  paths remain stable for provenance and tests; they are not live status.

Do not select work from an old markdown checkpoint. The generated current queue
and its source/input fingerprint are the authority.
