# Matching-C wave 121

F_C834 is now a matching-C owner. Its complete 67-byte voice reset compiles
from a deterministic Turbo C source unit, preserving the historical save
order, one call, seven data symbols and no DOS loader relocations. The fresh
object matches the prior ASM extent byte for byte.

Matching-C coverage is now 57,257 bytes across 335 owners. Matching-ASM
coverage is 1,638 bytes across 10 owners.
