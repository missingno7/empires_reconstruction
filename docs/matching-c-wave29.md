# Twenty-ninth local C wave: cursor blitter

F_2269 adds 72 matching C bytes. Its two blit paths, cursor-state decrement,
record-stride calculation and DS:96EE buffer reference all bind to exact
component or storage declarations. The fresh object reproduces the complete
extent and all 13 fixups.

Coverage is now 220 matching C routines / 26,420 bytes. The full EXE and both
DAT archives remain byte-identical.
