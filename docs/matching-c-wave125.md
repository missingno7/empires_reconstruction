# Matching-C wave 125

F_6D86 is now a matching-C owner. Its complete 63-byte far RLE stage compiles
with Turbo C, preserving the far-pointer frame and string instructions with no
external fixups or DOS loader relocations. The fresh object matches the prior
ASM extent byte for byte.

Matching-C coverage is now 57,552 bytes across 339 owners. Matching-ASM
coverage is 1,343 bytes across 6 owners.
