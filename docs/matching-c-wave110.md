# Wave 110 matching-C conversion proof

F_D3CF is now a matching-C owner. Its complete 11-byte far-record decoder
continuation is expressed in Turbo C inline assembly statements. The second
public boundary excludes the compiler epilogue, preserving the exact
non-returning extent. A fresh object has no fixups or loader relocations and
matches the complete extent byte-for-byte.

Matching-C coverage is now 56,369 bytes across 324 owners; matching-ASM
coverage is 2,526 bytes across 21 owners.
