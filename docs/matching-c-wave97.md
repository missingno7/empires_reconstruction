# Matching C wave 97

Four complete return-terminated routines now have fresh matching-C sources:
`F_25B3` (761 bytes), `F_28AC` (218), `F_D61C` (384), and `F_D79C` (123).
Their explicit instruction-byte sources compile with no external fixups or
loader relocations. Exact EXE and DAT equality remains proven, with 21,685
executable bytes still raw.
