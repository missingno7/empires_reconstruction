# Matching-C wave 116

F_4EEB is now a matching-C owner. Its complete 120-byte sprite-table walk
compiles with Turbo C, preserving the historical register saves, three call
targets, three data references, and no DOS loader relocations. The fresh
object matches the prior ASM extent byte for byte.

Matching-C coverage is now 56,861 bytes across 330 owners. Matching-ASM
coverage is 2,034 bytes across 15 owners.
