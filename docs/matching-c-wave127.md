# Matching-C wave 127

F_50D2 is now a matching-C owner. Its complete 237-byte display-adapter probe
compiles from a deterministic Turbo C source unit, preserving the hardware I/O
sequence, thirteen data fixups and no DOS loader relocations. The fresh object
matches the prior ASM extent byte for byte.

Matching-C coverage is now 57,905 bytes across 341 owners. Matching-ASM
coverage is 990 bytes across 4 owners.
