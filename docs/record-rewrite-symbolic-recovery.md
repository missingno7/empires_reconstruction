# F_643A symbolic record rewrite

`F_643A` is now a 240-byte symbolic TASM owner for the indexed persistent
record rewrite path. The recovered source makes the offset calculation,
seek/read/write/close sequence, retry condition, and post-write metadata
refresh explicit.

It declares all seven DGROUP data relationships and seven near code/library
relationships normally. Fresh TASM preserves the complete `_TEXT` contribution
and all 26 `_TEXT` fixup semantics of the former matching-C proof object.
The canonical link is retained only after whole-EXE verification.
