# Matching-C wave 126

F_C914 is now a matching-C owner. Its complete 116-byte music-stream decoder
compiles from a deterministic Turbo C source unit, preserving seven command
calls, four stream-state data symbols and no DOS loader relocations. The fresh
object matches the prior ASM extent byte for byte.

Matching-C coverage is now 57,668 bytes across 340 owners. Matching-ASM
coverage is 1,227 bytes across 5 owners.
