# Matching-C wave 123

F_D89A is now a matching-C owner. Its complete 86-byte rectangle hit-test
compiles with Turbo C, preserving the historical SI/DI frame, one table fixup
and no DOS loader relocations. The fresh object matches the prior ASM extent
byte for byte.

Matching-C coverage is now 57,402 bytes across 337 owners. Matching-ASM
coverage is 1,493 bytes across 8 owners.
