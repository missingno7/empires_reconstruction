# Matching-C wave 119

F_C27D is now a matching-C owner. Its complete 109-byte voice service loop
compiles from a deterministic Turbo C source unit, preserving the unpushed SI
cursor, ten data references, four calls and no DOS loader relocations. The
fresh object matches the prior ASM extent byte for byte.

Matching-C coverage is now 57,103 bytes across 333 owners. Matching-ASM
coverage is 1,792 bytes across 12 owners.
