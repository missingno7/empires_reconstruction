# Wave 113 matching-C conversion proof

F_4AA8 is now a matching-C owner. Its complete 100-byte rectangle clipping
routine compiles from Turbo C inline assembly with the compiler-generated
parameter frame and one established near-call fixup. The fresh object has no
loader relocations and matches the prior ASM extent exactly.

Matching-C coverage is now 56,539 bytes across 327 owners; matching-ASM
coverage is 2,356 bytes across 18 owners.
