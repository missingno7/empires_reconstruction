# Matching-C wave 122

F_D85F is now a matching-C owner. Its complete 59-byte record deletion and
`REP MOVSB` compaction compile with Turbo C, preserving the historical SI/DI
frame, two data fixups and no DOS loader relocations. The fresh object matches
the prior ASM extent byte for byte.

Matching-C coverage is now 57,316 bytes across 336 owners. Matching-ASM
coverage is 1,579 bytes across 9 owners.
