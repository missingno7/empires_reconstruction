# Matching-C wave 117

F_1ECD is now a matching-C owner. Its complete 74-byte far rectangle-queue
drain compiles with Turbo C, preserving three far data references, the F_039F
call, and no DOS loader relocations. The fresh object matches the prior ASM
extent byte for byte.

Matching-C coverage is now 56,935 bytes across 331 owners. Matching-ASM
coverage is 1,960 bytes across 14 owners.
