# Matching-C wave 120

F_CAF1 is now a matching-C owner. Its complete 87-byte sound tick service
body compiles from a deterministic Turbo C source unit, preserving the
historical register saves, twelve data symbols and no DOS loader relocations.
The fresh object matches the prior ASM extent byte for byte.

Matching-C coverage is now 57,190 bytes across 334 owners. Matching-ASM
coverage is 1,705 bytes across 11 owners.
