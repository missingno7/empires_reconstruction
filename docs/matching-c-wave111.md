# Wave 111 matching-C conversion proof

F_01A4 is now a matching-C owner. Its complete 22-byte DOS setup and handoff
sequence compiles from Turbo C inline assembly. The fresh object reproduces
both established near-call fixups, has no loader relocations, and matches the
complete prior ASM extent byte-for-byte.

Matching-C coverage is now 56,391 bytes across 325 owners; matching-ASM
coverage is 2,504 bytes across 20 owners.
